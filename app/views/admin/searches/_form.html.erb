<%= form_with :model => @search, url:admin_search_path, method: :get, local: true  do |f| %>

  <%= select_tag "search", options_for_select([["全て","all_match"],["対応中","responding_match"], ["承認","approval_match"], ["否認","denial_match"], ["終了","finish_match"]]) %>

  <%= submit_tag :絞り込む, class: "btn btn-secondary" %>
<% end %>