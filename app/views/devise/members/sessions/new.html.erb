<% @page_title = "ログインページ" %>

<div class="container">
<div class="text-center">

<h2>ログイン</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field"><br>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", :placeholder => "ご登録のメールアドレス"  %>
  </div>

  <div class="field"><br>
    <%= f.password_field :password, autocomplete: "current-password", :placeholder => "パスワード" %>
  </div>

  <% if devise_mapping.rememberable? %><br />
    <div class="field">
      <%#= f.check_box :remember_me %>
      <%#= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "ログイン" %>
  </div>
<% end %>

<h2>member1@example.com</h2>
<h2>worker</h2>


<%#= render "devise/members/shared/links" %>
<br>
<%= link_to '新規登録', new_member_registration_path, class: 'btn btn-primary' %>


</div>
</div>

